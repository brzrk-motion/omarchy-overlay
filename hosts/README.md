# Host profiles

Reserved for machine-specific configuration:

- monitor names and coordinates
- resolution / refresh rate
- scale
- VRR
- fixed workspace-to-monitor mappings (the shared two-monitor workspace pairs are dynamic)
- GPU-specific overrides

These do not belong in the shared creative-workstation layer.
